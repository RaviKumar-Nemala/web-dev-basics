// no need to use the # symbol in the brackets

document.getElementById("para").innerHTML = "hdf";

// if the two id's are same first tag will be selected 
document.getElementById("para").innerHTML = "ajfkalf";

// selects all the values matches with the provided class name

let values = document.getElementsByClassName("div_1");

values [ 0 ] .innerHTML  = "<h1>FKCK</h1>"

//used to extract the existed content 

let content = values [0].textContent;

console.log( content);




