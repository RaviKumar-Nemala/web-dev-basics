/* strict mode is used to write a more clean code*/

// it specifies some restrictions on the usage of the varaibles//


'use strict'

const temp = 1;

console.log( temp )

console.log( temp )
//interface may be the functionality in the future that is why stirct mode does not allow to execute this code

// with out strict mode it executes fine

var interface  = 33;

console.log( interface );


